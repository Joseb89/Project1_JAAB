spring:
  application:
    name: Project 1
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  datasource:
    username: postgres
    password: Elessar1989!
    url: jdbc:postgresql://35.192.3.233:5432/project1-jaab
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  
